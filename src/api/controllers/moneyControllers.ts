import { Request, Response } from "express";
import YearService from "../../services/moneyService";
import Container from "typedi";

export const getYearlyData = async (req: Request, res: Response) => {};
